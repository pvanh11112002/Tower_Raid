using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FieldController : MonoBehaviour
{
    //[SerializeField] Transform[] fieldCardSlots;
    //[SerializeField] Card cardPrefab;

    //private List<Card> fieldCards = new List<Card>();

    //public void InitField()
    //{
    //    fieldCards.Clear();
    //    for (int i = 0; i < fieldCardSlots.Length; i++)
    //    {
    //        Card card = InitCards(fieldCardSlots[i]);
    //        fieldCards.Add(card);
    //    }
    //}  
    //private Card InitCards(Transform parent)
    //{
    //    Card card = Instantiate(cardPrefab, parent);
    //    return card;
    //}    
}
